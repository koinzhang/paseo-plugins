# 008 · Tasks

- [x] Composer 输入框边框透明 — typecheck
- [x] Pill 按 computed style 签名标记并去边框（class 键缓存） — typecheck
- [x] Combobox / menu 桌面弹层边框改为 0.5px 发丝线，颜色为主题 `border` 50% 透明 — typecheck
- [x] typecheck / test / reload 验证 — 19 项测试通过，`paseo plugin ls` 为 `running`
- [ ] 手动验收：composer / pill / 弹层边框消失，切换主题后恢复
