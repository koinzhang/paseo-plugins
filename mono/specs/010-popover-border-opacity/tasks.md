# 010 · Tasks

- [x] 非 Mono 主题弹层边框按宿主颜色混合至 50% 透明，并清理内联变量 — `npm run typecheck` / `npm test`
- [x] 更新 Mono README 中的边框描述 — review
- [x] typecheck / test / reload 验证 — `npm run typecheck`、19 项测试通过；`paseo plugin reload mono` 后状态为 `running`
- [ ] 手动验收：非 Mono 主题的 combobox / menu，主题切换及关闭开关
