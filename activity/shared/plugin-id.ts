/** Runtime plugin id (paseo-plugin.json / install / data dir). */
export const PLUGIN_ID = "activity";

/** Pre–Phase-2 id; kept for one-shot data-dir migration. */
export const LEGACY_PLUGIN_ID = "tool-usage";

export const LOG_PREFIX = `[${PLUGIN_ID}]`;
