# 011 — Tasks

- [x] T1 `shared/usage.ts`：schema + `aggregateByProvider` 计入 `workspaceCount`。验证：单测跨 provider 去重。
- [x] T2 `client/global-surface.tsx`：KPI Total calls → Workspaces。验证：label/value 来源。
- [x] T3 单测覆盖跨 provider 去重；`npm run typecheck` / `npm test` 通过。
- [x] T4 勾选 `spec.md` 验收；更新 `specs/README.md`。
