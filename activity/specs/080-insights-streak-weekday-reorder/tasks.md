# Tasks

- [x] T1.1 `buildActivityInsights` 行顺序调整（shared/insights.ts）
- [x] T1.2 测试顺序断言更新（shared/insights.test.ts）
- [x] T1.3 `npm run typecheck` / `npm test`（254 pass）通过；`paseo plugin reload activity` 已执行（running）
