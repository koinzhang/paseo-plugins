# Plan

Update the `heatmap.title`, `creations.title`, and `timeline.title` entries in the shared English and Chinese message catalogs. The three chart components already render these entries, so no component changes are needed. Update the product README to use the new names.
