# 039 — tasks

- [x] T1 `formatActivityTime` + 别名；删 `formatDayTime`；`formatLocalDateTime` 默认 `en`；更新单测
- [x] T2 新增 `FormattedTime`；接线 popover / agent panel / workspace rank / agents meta
- [x] T3 typecheck + 单测 + `paseo plugin reload activity`
