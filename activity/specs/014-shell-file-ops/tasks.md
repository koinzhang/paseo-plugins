# 014 — Tasks

- [x] T1 写入本目录 spec / plan；更新 `specs/README.md` 索引
- [x] T2 `isShellCall` / `isFileRead` / `isFileWrite` + 修正 summarize / by-provider / shellTop；RPC zod 字段
- [x] T3 单测：互斥口径 + shellTop 排除 low-skill
- [x] T4 全局 KPI + Top shell commands；panel KPI
- [x] T5 typecheck / test；`paseo plugin reload activity`；勾选 spec 验收
