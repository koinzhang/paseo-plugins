# Tasks

- [x] Add the five-row collapsed view and accessible right-aligned toggle to `RankingBars`.
- [x] Add English and Chinese labels; `npm run typecheck` and `npm test` passed (254 tests).
- [x] Reload the local Activity plugin; the command reported `running`.
