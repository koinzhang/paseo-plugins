# Tasks

- [x] T1.1 `filterRow` 右对齐（client/global-surface.tsx）— `justifyContent: "flex-end"`
- [x] T1.2 Provider 下拉菜单右对齐展开（client/provider-dropdown.tsx）— 浮层锚点 `left: 0` → `right: 0`
- [x] T1.3 design-system §6 更新；`npm run typecheck` / `npm test`（254 pass）通过
- [ ] T1.4 `paseo plugin reload activity` 已执行（running）；待页面目测 Global（regular + compact）
