# Tasks

- [x] `global-surface.tsx`: `RankList` items are `lists[rankKind].slice(0, insights.length)`; dropped the local `LIST_LIMIT` alias. `npm run typecheck` and `npm test` passed (254 tests).
- [x] Reload the local Activity plugin; `paseo plugin ls` reported `running`.
