# 010 — Tasks

- [x] T1 `isActiveDay` + `computeStreaks` 含 messages/agents；更新 activity / messages 测试。验证：`shared/activity.test.ts`、`server/messages.test.ts`
- [x] T2 `buildActivityInsights` + 单测。验证：`shared/insights.test.ts`
- [x] T3 `global-surface` 接线；README 索引。验证：typecheck
- [x] T4 `npm run typecheck` && `npm test`（94/94）
- [x] T5 三列上限 8；移除 MCP failures。验证：insights ≤8、skills/mcp `LIST_LIMIT`、单测无 failures 行

