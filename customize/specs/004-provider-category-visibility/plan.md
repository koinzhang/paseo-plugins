# 方案

1. 在 shared 层建立分类可见性函数：已具备扫描位置的分类默认可见，对官方确认支持、但尚无可靠扫描位置的分类列出例外。
2. Surface 按函数过滤分类栏；切换时沿用当前分类，不可见时校正为第一个可见分类（见 012）。
3. 针对无 Rules/MCP/Commands 的 Provider、未扫描的插件能力和 Provider 切换编写单元测试。
4. 运行类型检查和测试，重载 Customize。
