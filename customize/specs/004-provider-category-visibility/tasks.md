# Tasks

- [x] 核对分类能力与扫描状态的区别（见 research.md）。
- [x] 实现按 Provider 过滤分类及切换时的选择校正（`shared/category-visibility.ts`、`client/surface.tsx`）。
- [x] 添加测试并运行 typecheck、test（29 项通过）。
- [x] 重载 Customize，确认运行状态（`paseo plugin reload customize` 为 `running`；日志最新记录 `Plugin ready`）。
