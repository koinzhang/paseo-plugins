# 方案

1. 使用 Paseo 为每个插件和 host 分配的 React Query client；沿用现有 `customize.scan` 的 Provider/Project 查询键，设置非活动数据不回收，并在组件挂载时无条件重新获取。
2. Provider 快照和项目列表同样保留缓存，让页面入口保持可显示；已有数据的后台刷新错误不替换整页或整组条目。
3. 保留手动重新扫描按钮和初次加载/失败状态；验证切换查询键、缓存显示和刷新失败的行为。
